1.xiaozhi-esp32/docs/mcp-protocol.md 

里面有一张MCP消息流程的交互图：

这个Readme里面介绍了用mqtt或websocket两种传输方式进行MACP交互，实现后台服务的内容



具体代码还需参照文件中内容

去找MCP payload里面相关vison的内容，格式为标准的json格式



2.xiaozhi-esp32/docs/websocket.md 

根据不同的消息格式进行处理

![1774253456861](C:\Users\81092\AppData\Roaming\Typora\typora-user-images\1774253456861.png)





语音部分的处理：xiaozhi-esp32\main\audio\processors\afe_audio_processor.cc

用于音频前端处理（AFE），基于 ESP-SR 库。它处理音频输入，通过任务循环应用回声消除（AEC）、噪声抑制（NS）、语音活动检测（VAD）等算法，并通过回调输出处理结果

## 整个摄像头以及照片过程

摄像头拍照识别链路。

板级初始化摄像头实例，见 m5stack_core_s3.cc:330。

MCP 注册工具 self.camera.take_photo，见 mcp_server.cc:102。

工具执行时先 Capture，再 Explain(question)，见 mcp_server.cc:119。

相机拍照入口是 esp32_camera.cc:381。

Explain 会把图片编码 JPEG 后用 multipart/form-data POST 到 explain_url，见 esp32_camera.cc:863。

explain_url 从哪里来。

后端在 MCP initialize 的 capabilities.vision.url 下发。

设备解析并写入 Camera 的 SetExplainUrl，见 mcp_server.cc:336。

Camera 保存 URL/Token，见 esp32_camera.cc:376。

MCP 回包怎么回到后端。

McpServer 处理 tools/call 后 ReplyResult，见 mcp_server.cc:440。

Application 通过主线程发送 MCP，见 application.cc:850。

Protocol 统一封装成 type=mcp JSON 发出去，见 protocol.cc:76。

GC0308 摄像头驱动及自动检测





### VAD 状态（Voice Activity Detection，语音活动检测）

VAD 用于检测音频流中是否有说话（语音活动）或静默。它是音频前端处理的一部分，帮助系统只在有语音时处理数据，避免浪费资源。

- **实现方式**：在 `afe_audio_processor.cc` 中，通过 ESP-SR 库的 AFE 接口实现。初始化时配置 VAD 模式（如 `VAD_MODE_0`），并在处理任务中检查结果。

- 相关代码位置

  ：

  - 初始化配置（第 11-67 行）：`afe_config->vad_mode = VAD_MODE_0;` 和 `afe_config->vad_init = true;`（条件编译下）。
  - 状态检测和回调（第 121-175 行，在 `AudioProcessorTask`）：检查 `res->vad_state`，如果是 `VAD_SPEECH` 或 `VAD_SILENCE`，调用 `vad_state_change_callback_`。
  - 回调设置（第 116-120 行）：`OnVadStateChange` 方法注册回调函数。

### 设备 AEC（Acoustic Echo Cancellation，声学回声消除）

AEC 用于消除音频中的回声（如扬声器播放的声音被麦克风拾取）。设备 AEC 指硬件加速的回声消除（如果芯片支持），否则使用软件实现。

- **实现方式**：通过 ESP-SR 库的 AFE 配置启用。初始化时根据宏 `CONFIG_USE_DEVICE_AEC` 决定启用硬件 AEC（禁用 VAD）或软件 VAD（禁用 AEC）。运行时可动态切换。

- 相关代码位置

  ：

  - 初始化配置（第 11-67 行）：条件编译 `#ifdef CONFIG_USE_DEVICE_AEC`，设置 `afe_config->aec_init = true;` 和 `afe_config->vad_init = false;`。
  - 动态切换（第 176-188 行，在 `EnableDeviceAec`）：调用 `afe_iface_->enable_aec` 或 `afe_iface_->disable_aec`，并相应切换 VAD。
  - 宏定义：`CONFIG_USE_DEVICE_AEC` 通常在 ESP-IDF 项目配置（[sdkconfig](vscode-file://vscode-app/d:/Microsoft VS Code/ce099c1ed2/resources/app/out/vs/code/electron-browser/workbench/workbench.html) 或 `menuconfig`）中定义，表示硬件支持。











