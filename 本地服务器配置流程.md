# 本地服务器配置流程

1.后端的url不是在客户端代码写死的，是对应设备启动后会先请求 OTA返回的结构里面的url

面按新手路线，一步一步来。

**第一步：先确认当前是走“云 OTA 下发配置”**

1. 你的默认 OTA 地址是云端，见 [sdkconfig:603](vscode-file://vscode-app/d:/Microsoft VS Code/ce099c1ed2/resources/app/out/vs/code/electron-browser/workbench/workbench.html) 和 [Kconfig.projbuild:3](vscode-file://vscode-app/d:/Microsoft VS Code/ce099c1ed2/resources/app/out/vs/code/electron-browser/workbench/workbench.html)。
2. 设备启动后会访问 OTA 地址，解析 mqtt/websocket 字段，见 [ota.cc:143](vscode-file://vscode-app/d:/Microsoft VS Code/ce099c1ed2/resources/app/out/vs/code/electron-browser/workbench/workbench.html) 和 [ota.cc:164](vscode-file://vscode-app/d:/Microsoft VS Code/ce099c1ed2/resources/app/out/vs/code/electron-browser/workbench/workbench.html)。
3. 如果 OTA 没返回 websocket/mqtt，代码会默认走 MQTT，见 [application.cc:410](vscode-file://vscode-app/d:/Microsoft VS Code/ce099c1ed2/resources/app/out/vs/code/electron-browser/workbench/workbench.html)。

**第二步：本地服务最小架构**
你本地至少要有 2 个接口：

1. OTA HTTP 接口
   作用：告诉设备去连哪个 websocket 地址。
2. WebSocket 对话接口
   作用：收语音、发文本和 TTS 音频。

最小 OTA 返回可以先这样（示例）：
{
"websocket": {
"url": "ws://192.168.1.100:8000/ws",
"token": "",
"version": 3
},
"server_time": {
"timestamp": 1760000000000,
"timezone_offset": 480
}
}

说明：

1. 只要带 websocket 对象，设备就会选 WebSocket 协议。
2. token 可先留空，后续再加鉴权。
3. version 建议和你服务实现一致，先用 3。

**第三步：把固件 OTA 地址改成你的本地地址**

1. 在 menuconfig 改 OTA_URL
   位置在 [Kconfig.projbuild:3](vscode-file://vscode-app/d:/Microsoft VS Code/ce099c1ed2/resources/app/out/vs/code/electron-browser/workbench/workbench.html) 对应的配置项。
2. 例如改成：
   [http://192.168.1.100:8000/xiaozhi/ota](vscode-file://vscode-app/d:/Microsoft VS Code/ce099c1ed2/resources/app/out/vs/code/electron-browser/workbench/workbench.html)
3. 重新 build + flash。

非常重要：
[ota.cc:44](vscode-file://vscode-app/d:/Microsoft VS Code/ce099c1ed2/resources/app/out/vs/code/electron-browser/workbench/workbench.html) 先读的是 NVS 里的 wifi.ota_url。
如果你之前已经连过云，NVS 里可能缓存旧地址，导致你改了配置仍访问云端。

遇到这种情况，直接做一次清空：

1. 执行 erase-flash
2. 再 flash
3. 重新配网

**第四步：WebSocket 服务要满足的最小协议**
设备端实现参考 [websocket_protocol.cc:83](vscode-file://vscode-app/d:/Microsoft VS Code/ce099c1ed2/resources/app/out/vs/code/electron-browser/workbench/workbench.html) 和文档 [websocket.md](vscode-file://vscode-app/d:/Microsoft VS Code/ce099c1ed2/resources/app/out/vs/code/electron-browser/workbench/workbench.html)。

你服务端最少要支持：

1. 收到设备 hello 后，回一个 type=hello 且 transport=websocket 的 JSON。
2. 能接收设备发来的二进制 Opus 音频帧。
3. 返回这些 JSON 消息让设备正常工作：

- stt（识别文本）
- llm（情绪）
- tts start / sentence_start / stop

1. 下发二进制 TTS 音频帧（Opus）给设备播放。

如果你只先测链路，不做完整语音，也可以先做“假响应”：

1. 设备发 listen start 后，立即返回一条 stt 文本和一条 tts stop。
2. 先验证消息闭环，再接入 ASR/LLM/TTS。

**第五步：验证是否成功切换到本地**
看日志里这些关键信息：

1. OTA 请求地址是否是你本地地址。
2. 是否打印连接 websocket 服务器地址，见 [websocket_protocol.cc:174](vscode-file://vscode-app/d:/Microsoft VS Code/ce099c1ed2/resources/app/out/vs/code/electron-browser/workbench/workbench.html)。
3. 是否收到 server hello（如果超时会报错），见 [websocket_protocol.cc:188](vscode-file://vscode-app/d:/Microsoft VS Code/ce099c1ed2/resources/app/out/vs/code/electron-browser/workbench/workbench.html)。

**第六步：接入本地大模型的推荐顺序**

1. 先打通设备 <-> 本地 OTA。
2. 再打通设备 <-> 本地 WebSocket 协议握手和消息。
3. 再接 ASR（语音转文本）。
4. 再接 LLM。
5. 最后接 TTS 音频回传。

这样每一步都可验证，最不容易卡死。

你现在这块我建议下一步直接做实操：
我可以给你生成一份“可直接跑的最小本地 OTA + WebSocket Python 服务模板”，你只需要改 IP 就能先连通设备。
如果你同意，我下一条就按你的 Windows 环境一步步给你跑起来

